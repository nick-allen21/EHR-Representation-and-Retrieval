\paragraph{Nicholas Allen (nallen21).}
Implemented and validated the data access and preprocessing infrastructure, including: acquiring credentialed dataset access, organizing BigQuery configuration and permissions, building the MIMIC extraction pipeline (notes + structured tables), and producing longitudinal patient timeline records suitable for downstream QA generation and model training.

\paragraph{Niki Yoon (nikiyoon).}
Designed the learned context selection architecture and modeling plan, including: chunking and weak-labeling strategy for (question, chunk) supervision, feature brainstorming/specification, initial logistic regression model design, and drafting this milestone report.